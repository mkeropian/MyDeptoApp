[build]
  command = "npm run build"
  publish = "dist/mydepto/browser"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
  NPM_VERSION = "10"
```

#### **3.5 - `.npmrc` para ambos proyectos**
```
engine-strict=false
legacy-peer-deps=false
